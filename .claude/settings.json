{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$CLAUDE_PROJECT_DIR/hooks/validate_finding.py\" || { echo 'validate_finding hook unavailable or errored — failing closed' >&2; exit 2; }"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$CLAUDE_PROJECT_DIR/hooks/guard_findings_bash.py\" || { echo 'findings bash guard unavailable or errored — failing closed' >&2; exit 2; }"
          }
        ]
      }
    ]
  }
}
